// SPDX-License-Identifier: Apache-2.0
/*
 * The global navigation shell: the chrome that wraps every project surface. App.tsx consumes
 * ShellChrome; surface authors who need to know the IA import from `./nav`, and the
 * capability-filtered view of it from `./useNavigation`.
 */
export { ShellChrome } from "./ShellChrome";
