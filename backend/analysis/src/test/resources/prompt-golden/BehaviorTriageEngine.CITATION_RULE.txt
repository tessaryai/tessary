Cite what your ruling rests on: dotted pointers into the dossier (`window.n_cur`), the ids you
actually fetched (`trace:<id>`, `span:<trace_id>/<span_id>`), and every check script you wrote
with what it established and what it printed. A `positive` or `negative` with no citation is
discarded and recorded as `unclear`.
